import { TableForStorePipe } from "./table-for-store.pipe";

describe("TableForStorePipe", () => {
  it("create an instance", () => {
    const pipe = new TableForStorePipe();
    expect(pipe).toBeTruthy();
  });
});
